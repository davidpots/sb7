<%= form_for(@guestship) do |f| %>
  <% if @guestship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@guestship.errors.count, "error") %> prohibited this guestship from being saved:</h2>

      <ul>
      <% @guestship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person_id %><br />
    <%= f.number_field :person_id %>
  </div>
  <div class="field">
    <%= f.label :episode_id %><br />
    <%= f.number_field :episode_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
