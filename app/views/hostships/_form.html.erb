<%= form_for(@hostship) do |f| %>
  <% if @hostship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hostship.errors.count, "error") %> prohibited this hostship from being saved:</h2>

      <ul>
      <% @hostship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person_id %><br />
    <%= f.number_field :person_id %>
  </div>
  <div class="field">
    <%= f.label :show_id %><br />
    <%= f.number_field :show_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
